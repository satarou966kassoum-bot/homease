import { PhasePlaceholderPage } from "../components/ui/PhasePlaceholderPage";

export function MessagesPage() {
  return (
    <PhasePlaceholderPage
      title="Messagerie"
      phase="Arrive en Phase 3"
      description="La messagerie interne entre clients et propriétaires (l'API existe déjà côté backend) sera branchée ici dans la prochaine phase, avec notifications en temps réel."
    />
  );
}
